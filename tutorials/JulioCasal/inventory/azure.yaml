# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: inventory
services:  
  app:
    language: dotnet
    project: ./src/inventory.AppHost/inventory.AppHost.csproj
    host: containerapp
